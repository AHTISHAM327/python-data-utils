# Week 2 Project Journal

**Days 1 & 2:** Built the foundational loading and cleaning functions (`data_loader.py` and `data_cleaner.py`). Learned how to audit missing data, handle nulls, convert datatypes, and optimize memory safely.x
Day 3 — chaining replaces intermediate variables with a single readable expression. .pipe() is how you call your own functions inside a chain.
